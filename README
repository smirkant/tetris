/***************************************************************
 * README file for the TETRIS.
 * It decribes the changes compare to last version.
 ***************************************************************/

The last version has some unknow bugs when using the linux timer, 
which calls the timeout callback function as a thread. In this version,
The linux timer is removed, and the gtk timer is being used.
